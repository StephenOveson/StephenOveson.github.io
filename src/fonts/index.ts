import localFont from 'next/font/local';

export const spiegel = localFont({ src: '../fonts/spiegel.otf', weight: "400", variable: "--font-spiegel" });
export const beaufort = localFont({ src: '../fonts/beaufort-bold.ttf', weight: "400", variable: "--font-beaufort" });